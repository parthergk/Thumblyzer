import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata = {
  title: "Thumblyzer",
  description: "Thumblyzer is a tool designed to help YouTube content creators easily analyze and replicate successful thumbnail designs. By simply entering a YouTube video URL, Thumblyzer retrieves the video’s thumbnail and provides detailed information on its design elements, including font size, family, color, style, background color, and more",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
